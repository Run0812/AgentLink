/**
 * Re-export the AgentAdapter interface for convenient imports.
 *
 * ```ts
 * import type { AgentAdapter } from '../adapters/agent-adapter';
 * ```
 */
export type { AgentAdapter, AgentInput, StreamHandlers, AgentStatus } from '../core/types';
